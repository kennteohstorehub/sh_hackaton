<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <!-- StoreHub Design System -->
  <link rel="stylesheet" href="/css/storehub-design-system.css?v=<%= Date.now() %>">
  <link rel="stylesheet" href="/css/storehub-global.css?v=<%= Date.now() %>">
  <link rel="stylesheet" href="/css/storehub-backoffice-redesign.css?v=<%= Date.now() %>">
  <script src="/js/storehub-design-system.js?v=<%= Date.now() %>" defer></script>
  <%- csrfMeta() %>
  <style>
    body {
      background: linear-gradient(135deg, var(--sh-base-black) 0%, var(--sh-gray-900) 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .auth-container {
      background: white;
      border-radius: var(--sh-radius-lg);
      box-shadow: var(--sh-shadow-xl);
      width: 100%;
      max-width: 420px;
      overflow: hidden;
    }
    
    .auth-header {
      background: var(--sh-primary-orange);
      color: white;
      padding: var(--sh-space-8) var(--sh-space-6);
      text-align: center;
      position: relative;
    }
    
    .auth-badge {
      position: absolute;
      top: var(--sh-space-3);
      right: var(--sh-space-3);
      background: rgba(255, 255, 255, 0.2);
      color: white;
      padding: var(--sh-space-1) var(--sh-space-3);
      border-radius: var(--sh-radius-full);
      font-size: var(--sh-tiny);
      font-weight: var(--sh-weight-semibold);
      text-transform: uppercase;
    }
    
    .auth-logo {
      font-size: var(--sh-heading-1);
      font-weight: var(--sh-weight-bold);
      margin-bottom: var(--sh-space-2);
    }
    
    .auth-subtitle {
      font-size: var(--sh-body);
      opacity: 0.9;
      color: rgba(255, 255, 255, 0.9);
    }
    
    /* Merchant Badge - Subtle Style */
    .merchant-badge {
      background: linear-gradient(135deg, rgba(250, 140, 22, 0.08), rgba(212, 107, 8, 0.08));
      color: var(--sh-primary-pressed);
      border: 1px solid rgba(250, 140, 22, 0.2);
      padding: var(--sh-space-4) var(--sh-space-5);
      text-align: center;
      border-radius: var(--sh-radius-lg) var(--sh-radius-lg) 0 0;
    }
    
    .merchant-badge h3 {
      font-size: 20px;
      font-weight: 700;
      margin: 0;
      color: #D46B08;
    }
    
    .merchant-badge p {
      font-size: 14px;
      margin: 4px 0 0 0;
      opacity: 0.85;
      color: #FA8C16;
    }
    
    .auth-body {
      padding: var(--sh-space-8) var(--sh-space-6);
    }
    
    .auth-title {
      font-size: var(--sh-heading-3);
      font-weight: var(--sh-weight-semibold);
      color: var(--sh-gray-900);
      margin-bottom: var(--sh-space-2);
      text-align: center;
    }
    
    .auth-description {
      text-align: center;
      color: var(--sh-gray-600);
      margin-bottom: var(--sh-space-6);
      font-size: var(--sh-small);
    }
    
    .form-group {
      margin-bottom: var(--sh-space-5);
    }
    
    .form-label {
      display: block;
      margin-bottom: var(--sh-space-2);
      font-size: var(--sh-small);
      font-weight: var(--sh-weight-medium);
      color: var(--sh-gray-700);
    }
    
    .form-control {
      width: 100%;
      height: 48px;
      padding: var(--sh-space-3) var(--sh-space-4);
      font-size: var(--sh-body);
      border: 1px solid var(--sh-gray-400);
      border-radius: var(--sh-radius-md);
      transition: all 0.2s ease;
      background-color: white;
    }
    
    .form-control:focus {
      border-color: var(--sh-primary-orange);
      box-shadow: 0 0 0 3px rgba(250, 140, 22, 0.1);
      outline: none;
    }
    
    .btn-auth {
      width: 100%;
      height: 48px;
      margin-top: var(--sh-space-6);
      font-size: var(--sh-body);
      font-weight: var(--sh-weight-semibold);
      background: var(--sh-primary-orange);
      color: white;
      border: none;
      border-radius: var(--sh-radius-md);
      cursor: pointer;
      transition: all 0.25s cubic-bezier(0.25, 0.1, 0.25, 1);
      box-shadow: var(--sh-shadow-sm);
    }
    
    .btn-auth:hover {
      background: var(--sh-primary-hover);
      transform: translateY(-2px);
      box-shadow: var(--sh-shadow-md);
    }
    
    .btn-auth:active {
      background: var(--sh-primary-pressed);
      transform: translateY(0);
      box-shadow: none;
    }
    
    .security-notice {
      background: var(--sh-gray-100);
      border: 1px solid var(--sh-gray-300);
      border-radius: var(--sh-radius-md);
      padding: var(--sh-space-3);
      margin-top: var(--sh-space-6);
      font-size: var(--sh-small);
      color: var(--sh-gray-700);
      text-align: center;
    }
    
    .security-notice-icon {
      display: inline-block;
      margin-right: var(--sh-space-2);
    }
    
    .alert {
      margin-bottom: var(--sh-space-4);
    }
    
    .auth-footer {
      text-align: center;
      margin-top: var(--sh-space-6);
      padding-top: var(--sh-space-6);
      border-top: 1px solid var(--sh-gray-300);
      font-size: var(--sh-small);
      color: var(--sh-gray-600);
    }
    
    /* Loading state */
    .btn-auth:disabled {
      opacity: 0.6;
      cursor: not-allowed;
    }
    
    .spinner {
      display: inline-block;
      width: 16px;
      height: 16px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      border-top-color: white;
      border-radius: 50%;
      animation: spin 0.8s linear infinite;
      margin-right: var(--space-2);
    }
  </style>
</head>
<body>
  <div class="auth-container fade-in">
    <% if (typeof merchantName !== 'undefined' && merchantName) { %>
      <!-- Merchant Badge - Subtle Style -->
      <div class="merchant-badge">
        <h3><%= merchantName %></h3>
        <p><%= portalType %></p>
      </div>
    <% } else { %>
      <!-- Fallback for legacy -->
      <div class="auth-header">
        <span class="auth-badge">Admin</span>
        <div class="auth-logo">StoreHub</div>
        <div class="auth-subtitle">BackOffice Portal</div>
      </div>
    <% } %>
    
    <div class="auth-body">
      
      <% if (messages && messages.error) { %>
      <div class="alert alert-danger">
        <%= messages.error %>
      </div>
      <% } %>
      
      <% if (messages && messages.success) { %>
      <div class="alert alert-success">
        <%= messages.success %>
      </div>
      <% } %>
      
      <% if (messages && messages.info) { %>
      <div class="alert alert-info">
        <%= messages.info %>
      </div>
      <% } %>
      
      <form action="/backoffice/auth/login" method="POST" class="needs-validation" novalidate id="loginForm">
        <%- csrfInput() %>
        
        <div class="form-group">
          <label for="email" class="form-label">Email Address</label>
          <input 
            type="email" 
            class="form-control" 
            id="email" 
            name="email" 
            required 
            autofocus
            placeholder="admin@storehub.com"
            autocomplete="username"
          >
          <div class="invalid-feedback">
            Please enter a valid email address.
          </div>
        </div>
        
        <div class="form-group">
          <label for="password" class="form-label">Password</label>
          <input 
            type="password" 
            class="form-control" 
            id="password" 
            name="password" 
            required
            placeholder="Enter your password"
            autocomplete="current-password"
          >
          <div class="invalid-feedback">
            Please enter your password.
          </div>
        </div>
        
        <button type="submit" class="btn btn-auth" id="submitBtn">
          Sign In to BackOffice
        </button>
      </form>
      
      <div class="security-notice">
        <span class="security-notice-icon">🔒</span>
        This is a secure area. All activities are logged and monitored.
      </div>
      
      <div class="auth-footer">
        <p>StoreHub BackOffice v2.0</p>
        <p>© <%= new Date().getFullYear() %> StoreHub. All rights reserved.</p>
      </div>
    </div>
  </div>
  
  <script>
    // Form validation and loading state
    (function() {
      'use strict';
      
      const form = document.getElementById('loginForm');
      const submitBtn = document.getElementById('submitBtn');
      const originalBtnText = submitBtn.innerHTML;
      
      form.addEventListener('submit', function(event) {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        } else {
          // Show loading state
          submitBtn.disabled = true;
          submitBtn.innerHTML = '<span class="spinner"></span> Authenticating...';
        }
        
        form.classList.add('was-validated');
      }, false);
      
      // Auto-focus email field
      window.addEventListener('load', function() {
        const emailField = document.getElementById('email');
        if (emailField && !emailField.value) {
          emailField.focus();
        }
      });
    })();
  </script>
</body>
</html>