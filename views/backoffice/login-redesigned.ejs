<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Backoffice Login - StoreHub</title>
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <!-- StoreHub Design System -->
    <link rel="stylesheet" href="/css/storehub-backoffice-system.css">
    
    <style>
        body.backoffice-login {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #1C1C1C 0%, #2a2a2a 100%);
            position: relative;
            overflow: hidden;
        }
        
        /* Animated background pattern */
        body.backoffice-login::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                radial-gradient(circle at 20% 50%, rgba(250, 140, 22, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(250, 140, 22, 0.05) 0%, transparent 50%);
            animation: float 20s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(-20px, -20px) rotate(1deg); }
            66% { transform: translate(20px, -10px) rotate(-1deg); }
        }
        
        .login-container {
            width: 100%;
            max-width: 420px;
            padding: var(--space-6);
            position: relative;
            z-index: 1;
        }
        
        .login-card {
            background: var(--base-white);
            border-radius: var(--radius-xl);
            padding: var(--space-10);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            animation: slideUp 0.5s ease-out;
        }
        
        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .login-header {
            text-align: center;
            margin-bottom: var(--space-10);
        }
        
        .login-logo {
            width: 64px;
            height: 64px;
            margin: 0 auto var(--space-4);
            background: var(--primary-orange);
            border-radius: var(--radius-lg);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--base-white);
            font-size: 28px;
            box-shadow: 0 4px 12px rgba(250, 140, 22, 0.3);
        }
        
        .login-title {
            font-size: var(--heading-2);
            font-weight: var(--weight-bold);
            color: var(--gray-900);
            margin-bottom: var(--space-2);
        }
        
        .login-subtitle {
            font-size: var(--body);
            color: var(--gray-600);
        }
        
        .login-form {
            margin-bottom: var(--space-6);
        }
        
        .form-floating {
            position: relative;
            margin-bottom: var(--space-5);
        }
        
        .form-floating input {
            width: 100%;
            height: 56px;
            padding: var(--space-4) var(--space-4) var(--space-2);
            font-size: var(--body);
            border: 1px solid var(--gray-300);
            border-radius: var(--radius-md);
            background: var(--base-white);
            transition: all var(--transition-fast);
        }
        
        .form-floating label {
            position: absolute;
            top: 50%;
            left: var(--space-4);
            transform: translateY(-50%);
            font-size: var(--body);
            color: var(--gray-500);
            pointer-events: none;
            transition: all var(--transition-fast);
        }
        
        .form-floating input:focus,
        .form-floating input:not(:placeholder-shown) {
            padding-top: var(--space-6);
            padding-bottom: var(--space-2);
            border-color: var(--primary-orange);
            box-shadow: 0 0 0 3px rgba(250, 140, 22, 0.1);
        }
        
        .form-floating input:focus ~ label,
        .form-floating input:not(:placeholder-shown) ~ label {
            top: var(--space-3);
            transform: translateY(0);
            font-size: var(--small);
            color: var(--primary-orange);
        }
        
        .form-floating input:focus {
            outline: none;
        }
        
        .form-options {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: var(--space-6);
        }
        
        .form-checkbox {
            display: flex;
            align-items: center;
            gap: var(--space-2);
        }
        
        .form-checkbox input[type="checkbox"] {
            width: 18px;
            height: 18px;
            accent-color: var(--primary-orange);
        }
        
        .form-checkbox label {
            font-size: var(--small);
            color: var(--gray-700);
            cursor: pointer;
        }
        
        .forgot-link {
            font-size: var(--small);
            color: var(--primary-orange);
            text-decoration: none;
            font-weight: var(--weight-medium);
            transition: color var(--transition-fast);
        }
        
        .forgot-link:hover {
            color: var(--primary-hover);
            text-decoration: underline;
        }
        
        .login-button {
            width: 100%;
            height: 52px;
            background: var(--primary-orange);
            color: var(--base-white);
            border: none;
            border-radius: var(--radius-md);
            font-size: var(--body);
            font-weight: var(--weight-semibold);
            cursor: pointer;
            transition: all var(--transition-fast);
            box-shadow: 0 4px 12px rgba(250, 140, 22, 0.3);
            position: relative;
            overflow: hidden;
        }
        
        .login-button:hover {
            background: var(--primary-hover);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(250, 140, 22, 0.4);
        }
        
        .login-button:active {
            background: var(--primary-pressed);
            transform: translateY(0);
        }
        
        .login-button:disabled {
            background: var(--gray-300);
            color: var(--gray-600);
            cursor: not-allowed;
            box-shadow: none;
        }
        
        .login-divider {
            text-align: center;
            margin: var(--space-8) 0;
            position: relative;
        }
        
        .login-divider::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 1px;
            background: var(--gray-300);
        }
        
        .login-divider span {
            background: var(--base-white);
            padding: 0 var(--space-4);
            position: relative;
            font-size: var(--small);
            color: var(--gray-600);
        }
        
        .social-login {
            display: flex;
            gap: var(--space-3);
        }
        
        .social-button {
            flex: 1;
            height: 44px;
            border: 1px solid var(--gray-300);
            background: var(--base-white);
            border-radius: var(--radius-md);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: var(--space-2);
            font-size: var(--small);
            color: var(--gray-700);
            cursor: pointer;
            transition: all var(--transition-fast);
        }
        
        .social-button:hover {
            background: var(--gray-100);
            border-color: var(--gray-400);
        }
        
        .login-footer {
            text-align: center;
            margin-top: var(--space-8);
            padding-top: var(--space-6);
            border-top: 1px solid var(--gray-200);
        }
        
        .login-footer p {
            font-size: var(--small);
            color: var(--gray-600);
        }
        
        .login-footer a {
            color: var(--primary-orange);
            text-decoration: none;
            font-weight: var(--weight-medium);
        }
        
        .login-footer a:hover {
            text-decoration: underline;
        }
        
        .error-message {
            background: rgba(207, 19, 34, 0.1);
            border: 1px solid var(--error-red);
            color: var(--error-red);
            padding: var(--space-3);
            border-radius: var(--radius-md);
            margin-bottom: var(--space-4);
            font-size: var(--small);
            display: flex;
            align-items: center;
            gap: var(--space-2);
        }
        
        .success-message {
            background: rgba(82, 196, 26, 0.1);
            border: 1px solid var(--success-green);
            color: var(--success-green);
            padding: var(--space-3);
            border-radius: var(--radius-md);
            margin-bottom: var(--space-4);
            font-size: var(--small);
            display: flex;
            align-items: center;
            gap: var(--space-2);
        }
        
        .spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top-color: var(--base-white);
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        @media (max-width: 480px) {
            .login-container {
                padding: var(--space-4);
            }
            
            .login-card {
                padding: var(--space-6);
            }
            
            .login-title {
                font-size: var(--heading-3);
            }
        }
    </style>
</head>
<body class="backoffice backoffice-login">
    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <div class="login-logo">
                    <i class="bi bi-shop"></i>
                </div>
                <h1 class="login-title">Welcome Back</h1>
                <p class="login-subtitle">Sign in to StoreHub Backoffice</p>
            </div>
            
            <% if (locals.error) { %>
                <div class="error-message">
                    <i class="bi bi-exclamation-circle"></i>
                    <%= error %>
                </div>
            <% } %>
            
            <% if (locals.success) { %>
                <div class="success-message">
                    <i class="bi bi-check-circle"></i>
                    <%= success %>
                </div>
            <% } %>
            
            <form class="login-form" method="POST" action="/backoffice/login" id="loginForm">
                <div class="form-floating">
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        placeholder=" "
                        required
                        autocomplete="email"
                    >
                    <label for="email">Email address</label>
                </div>
                
                <div class="form-floating">
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        placeholder=" "
                        required
                        autocomplete="current-password"
                    >
                    <label for="password">Password</label>
                </div>
                
                <div class="form-options">
                    <div class="form-checkbox">
                        <input type="checkbox" id="remember" name="remember">
                        <label for="remember">Remember me</label>
                    </div>
                    <a href="/backoffice/forgot-password" class="forgot-link">Forgot password?</a>
                </div>
                
                <button type="submit" class="login-button" id="loginButton">
                    <span id="buttonText">Sign In</span>
                    <span id="buttonSpinner" class="spinner" style="display: none;"></span>
                </button>
            </form>
            
            <div class="login-divider">
                <span>OR</span>
            </div>
            
            <div class="social-login">
                <button class="social-button" onclick="alert('SSO integration coming soon')">
                    <i class="bi bi-google"></i>
                    Google
                </button>
                <button class="social-button" onclick="alert('SSO integration coming soon')">
                    <i class="bi bi-microsoft"></i>
                    Microsoft
                </button>
            </div>
            
            <div class="login-footer">
                <p>
                    Don't have an account? 
                    <a href="/backoffice/register">Contact Administrator</a>
                </p>
            </div>
        </div>
    </div>
    
    <script>
        // Form submission with loading state
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            const button = document.getElementById('loginButton');
            const buttonText = document.getElementById('buttonText');
            const buttonSpinner = document.getElementById('buttonSpinner');
            
            // Show loading state
            button.disabled = true;
            buttonText.style.display = 'none';
            buttonSpinner.style.display = 'inline-block';
        });
        
        // Auto-focus first input
        document.getElementById('email').focus();
        
        // Animate form fields on load
        document.querySelectorAll('.form-floating').forEach((field, index) => {
            field.style.opacity = '0';
            field.style.transform = 'translateY(20px)';
            setTimeout(() => {
                field.style.transition = 'all 0.3s ease';
                field.style.opacity = '1';
                field.style.transform = 'translateY(0)';
            }, 100 * (index + 1));
        });
    </script>
</body>
</html>