<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organization Not Found - StoreHub QMS</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        .error-container {
            background: white;
            padding: 2.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 90%;
        }
        
        .error-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
        
        h1 {
            color: #FF8C00;
            font-size: 2rem;
            margin-bottom: 1rem;
        }
        
        p {
            color: #666;
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }
        
        .subdomain {
            font-family: monospace;
            background: #f0f0f0;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            font-size: 0.9rem;
        }
        
        .actions {
            margin-top: 2rem;
        }
        
        .button {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            background-color: #FF8C00;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 500;
            transition: background-color 0.3s ease;
        }
        
        .button:hover {
            background-color: #E67E00;
        }
        
        .contact {
            margin-top: 2rem;
            font-size: 0.9rem;
            color: #888;
        }
        
        .contact a {
            color: #FF8C00;
            text-decoration: none;
        }
        
        .contact a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">🏢</div>
        <h1>Organization Not Found</h1>
        <p>
            <% if (locals.subdomain) { %>
                The organization <span class="subdomain"><%= subdomain %></span> doesn't exist or is no longer active.
            <% } else { %>
                <%= locals.message || 'The requested organization could not be found.' %>
            <% } %>
        </p>
        <p>
            Please check the URL and try again, or contact your organization administrator.
        </p>
        <div class="actions">
            <a href="https://storehubqms.com" class="button">Visit Main Site</a>
        </div>
        <div class="contact">
            Need help? <a href="mailto:support@storehubqms.com">Contact Support</a>
        </div>
    </div>
</body>
</html>